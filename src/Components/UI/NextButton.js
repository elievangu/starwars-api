//npm import
import React from 'react';

//local import

const NextButton = () => {
  return (
    <button
      className="btn-floating btn-large cyan pulse next"
    >
    +
    </button>
  )
}

export default NextButton;